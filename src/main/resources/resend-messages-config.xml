<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context-3.2.xsd">

    <context:property-placeholder location="classpath:global-config.properties" />
    <bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:global-config.properties</value>
            </list>
        </property>
    </bean>

    <bean id="taskResendMessage" class="org.guce.siat.common.job.TaskResendMessage">
        <property name="fileProducer" ref="fileProducer" />
        <property name="messagesFolder" value="${messages.folder}" />
    </bean>

    <!-- specifing class and method that is going to be called on a specified time basis -->
    <bean id="jobResendMessage" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="targetObject" ref="taskResendMessage" />
        <property name="targetMethod" value="resendMessage" />
    </bean>
    <!-- toutes les 30 secondes -->
    <bean id="cronTriggerResendMsgs" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">
        <property name="jobDetail" ref="jobResendMessage" />
        <property name="cronExpression" value="1/5 0 0 ? * * *" />
    </bean>

    <!-- scheduler factory bean to bind,the executing code and time intervals together -->
    <bean class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
        <property name="jobDetails">
            <list>
                <ref bean="jobResendMessage" />
            </list>
        </property>
        <property name="triggers">
            <list>
                <ref bean="cronTriggerResendMsgs" />
            </list>
        </property>
    </bean>
</beans>




























